---
name: cut-slence
description: Alias of cut-silence. Detect silence and auto-trim video using HyperFrames rendering.
---

# $cut-slence

Alias of `$cut-silence`.

## Command

```bash
scripts/cut-slence --input <input.mp4> --output <output.mp4>
```

Supports the same options as `$cut-silence`.
